{
  "private": true,
  "engines": {
    "node": "16",
    "npm": "8"
  },
  "dependencies": {
    "serve": "^13.0.2"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "prettier-plugin-sh": "^0.10.2"
  },
  "scripts": {
    "format": "prettier --ignore-unknown --check '**'",
    "format:fix": "npm run format -- --write",
    "start": "serve src"
  }
}
